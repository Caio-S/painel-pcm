services:
  - type: web
    name: painel-pcm
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: MARIADB_HOST
        sync: false
      - key: MARIADB_PORT
        value: "3306"
      - key: MARIADB_USER
        sync: false
      - key: MARIADB_PASS
        sync: false
      - key: MARIADB_DB
        sync: false
      - key: PCM_EMPRESAS
        value: "8"
